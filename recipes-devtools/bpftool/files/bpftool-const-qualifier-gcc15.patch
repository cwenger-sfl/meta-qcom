Upstream-Status: Pending

GCC 15 diagnoses the assignments from the const-qualified results of
`strstr()` and `strchr()`. These pointers are only used for read-only
pointer arithmetic, so preserve the qualifiers instead of suppressing the
warning globally.

--- a/tools/lib/bpf/libbpf.c
+++ b/tools/lib/bpf/libbpf.c
@@ -8190 +8190 @@ static int kallsyms_cb(unsigned long long sym_addr, char sym_type,
-	char *res;
+	const char *res;
@@ -11495,1 +11495,2 @@ static int avail_kallsyms_cb(unsigned long long sym_addr, char sym_type,
-		char sym_trim[256], *psym_trim = sym_trim, *sym_sfx;
+		char sym_trim[256], *psym_trim = sym_trim;
+		const char *sym_sfx;
@@ -12080 +12080 @@ static int resolve_full_path(const char *file, char *result, size_t result_sz)
-			char *next_path;
+			const char *next_path;
